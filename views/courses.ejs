<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Course Detail</title>
</head>
<body>
    <% let { firstName, lastName, university } = course.teacher.details %>
    <h2><%= course.name %></h2> 

    <% if (course.teacher.role === 'teacher') { %>
        <p>Mentor : <%= firstName %> <%= lastName %> (<%= university %>)</p>
        
    <% } %>
    <p> 
        <a href="/teacher/<%= course.TeacherId %>"> detail teacher</a>
    </p>

    <div style="padding: 50px;">
        <p style="white-space: pre-line;"><%= course.description %></p>
    </div>
    
    <a href="/dashboard/course/<%= id %>/details/<%= course.id %>/unenroll">Unenroll</a>
</body>
</html>