<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <style>
        .profile {
            box-shadow: 0 4px 8px 0 rgba(0,0,0,0.2);
            transition: 0.3s;
            min-height: auto;
            min-width: 300px;
            max-height: auto;
            max-width: 300px;
            padding: 2px 16px;
            margin: auto;

        }
        .card {
            box-shadow: 0 4px 8px 0 rgba(0,0,0,0.2);
            transition: 0.3s;
            min-height: auto;
            min-width: 300px;
            max-height: auto;
            max-width: 300px;
            margin-right: 20px;
            float: left;
        }

        .card:hover {
            box-shadow: 0 8px 16px 0 rgba(0,0,0,0.2);
        }

        .container {
            padding: 2px 16px;
            margin: auto;
        }
        
    </style>
</head>
<body>
    <h1>Enroll to a course</h1>
    <div>Sort berdasarkan waktu : <a href="">terlama</a> | <a href="">terpendek</a></div>
    <div>Filter</div>

    <div class="container">
        <% courses.forEach(course => { %>
            <div class="card">
                <img style="max-width: 100%;" src="<%= course.image %>" alt="">
                    
                <div style="padding: 10px;">
                    <h3><%= course.name %> </h3>
                    <p><%= course.category %></p>
                    <p><%= course.duration %></p>
                    <a href="">enroll</a>
                </div>
               
            </div>
        <% }) %>
    </div>
</body>
</html>